#include <string>
using std::string;

class GradeBo{
	private:
		string courseName;
	public:
		GradeBook(string);
		void setCourseName(string);
		string getCourseName();
		void displayMessage();
		void determineClassAverage();
};
